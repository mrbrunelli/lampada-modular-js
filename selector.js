const acesa = document.querySelector('img#acesa')
const apagada = document.querySelector('img#apagada')
const titulo = document.querySelector('h1#titulo')

const lampada = {
    acesa,
    apagada,
    titulo
}

export default lampada
